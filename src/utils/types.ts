export type EventType = { target: { value: string } }

export type Action = {
  text: string
  icon: string
  type: string
  duration: string
  pointsEarned: string
  route: string
  actionTitle: string
  actionText: string
}
